# Sdk

Serves as the integration layer for third-party consumers by providing Data Transfer Objects (DTOs) and HTTP client connections to the API.

> Publish Sdk NuGet package so other csharp clients can connect to your API.